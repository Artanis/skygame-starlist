# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 4.2", :group => :test

gem "jekyll-theme-tactile", "~> 0.2.0", :group => :test
